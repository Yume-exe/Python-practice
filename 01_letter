from datetime import date 
today = date.today()
d = today.strftime("%B, %d, %Y")
name = input ("Please enter your name: ")
letter = '''Dear <Name>,
You are selected!
<Date>'''
print(letter.replace("<Name>", name).replace("<Date>", d))